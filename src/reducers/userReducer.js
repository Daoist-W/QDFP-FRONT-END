import C from "../constants";

export const user = (state={}, action) => {
    switch(action.type) {
        case C.CREATE_USER:
            //TODO: implement me
        default:
            return state;
    }
}
